# 코일 자동차감 시스템 - 추가 자동화 기능 제안서

**작성일**: 2025-01-21  
**버전**: 1.0  
**상태**: 제안 단계

---

## 📋 목차

1. [개요](#개요)
2. [우선순위별 자동화 기능](#우선순위별-자동화-기능)
3. [구현 상세](#구현-상세)
4. [예상 효과](#예상-효과)

---

## 개요

현재 시스템에 추가로 자동화할 수 있는 기능들을 분석하고 제안합니다. 이를 통해 사용자 입력을 더욱 줄이고 작업 효율성을 높일 수 있습니다.

---

## 우선순위별 자동화 기능

### 🔥 우선순위 1: 즉시 구현 권장

#### 1. 과거 수율 기반 자동 산출수량 계산

**현재 상태**: 사용자가 투입수량과 산출수량을 모두 입력해야 함

**제안 기능**: 투입수량 입력 시 과거 평균 수율을 기반으로 산출수량 자동 계산 및 제안

**작동 방식**:
1. 투입재료와 산출제품 선택
2. 투입수량 입력
3. 시스템이 과거 동일 조합의 평균 수율 조회
4. 산출수량 자동 계산: `투입수량 × (평균 수율 / 100)`
5. 사용자가 확인 후 수정 가능

**예시**:
- 투입수량: 100 입력
- 과거 평균 수율: 95%
- 자동 계산된 산출수량: 95 (회색으로 표시, 수정 가능)

**기대 효과**:
- ✅ 입력 시간 50% 단축
- ✅ 수율 일관성 향상
- ✅ 실수 방지

---

#### 2. 코일 스펙 기반 자동 투입수량 계산

**현재 상태**: 코일 스펙이 있어도 수동으로 투입수량 계산

**제안 기능**: 코일 스펙이 있는 경우 자동으로 투입수량 계산

**작동 방식**:
1. 투입재료로 코일 선택
2. 코일 스펙 조회 (길이, 폭, 두께, SEP 등)
3. 코일 스펙 기반 자동 계산:
   - 코일 총 중량 / EA중량 = 투입 가능 수량
   - 또는 사용자가 입력한 중량 기반 계산
4. 산출수량도 자동 계산

**예시**:
- 코일 선택: "냉연 코일 1.2T"
- 코일 스펙: 길이 1000mm, 폭 500mm, 두께 1.2mm, SEP 10
- EA중량: 0.47kg/개 (자동 계산)
- 코일 총 중량: 1000kg
- 자동 계산된 투입수량: 2,128개 (1000kg ÷ 0.47kg)

**기대 효과**:
- ✅ 코일 작업 시 계산 시간 80% 단축
- ✅ 계산 오류 방지
- ✅ 코일 스펙 활용도 향상

---

### ⚡ 우선순위 2: 단기 구현 권장

#### 3. 작업 완료 시 다음 공정 자동 생성

**현재 상태**: `auto_next_operation` 필드가 있으나 자동 생성 미구현

**제안 기능**: 작업 완료 시 다음 공정을 자동으로 생성

**작동 방식**:
1. 공정 작업 완료 처리
2. `auto_next_operation = true`인 경우
3. `next_operation_type` 확인 (예: "PRESS")
4. 다음 공정 자동 생성:
   - 투입재료: 현재 작업의 산출제품
   - 산출제품: BOM 또는 설정에서 자동 결정
   - 투입수량: 현재 작업의 산출수량
   - 산출수량: 평균 수율 기반 자동 계산
   - 상태: PENDING

**예시**:
- Blanking 공정 완료 (시트 95개 산출)
- 자동으로 Press 공정 생성:
  - 투입재료: 시트 (95개)
  - 산출제품: 부자재
  - 산출수량: 90개 (평균 수율 95% 기반)

**기대 효과**:
- ✅ 공정 연계 작업 시간 70% 단축
- ✅ 작업 흐름 자동화
- ✅ 누락 방지

---

#### 4. 재고 부족 시 자동 알림

**현재 상태**: 재고 알림 기능은 있으나 공정 작업 등록 시 실시간 알림 없음

**제안 기능**: 공정 작업 등록 시 재고 부족 자동 감지 및 알림

**작동 방식**:
1. 공정 작업 등록 시도
2. 투입재료 재고 확인
3. 재고 부족 시:
   - 경고 메시지 표시
   - 부족 수량 표시
   - 안전 재고 대비 부족량 표시
4. 사용자가 확인 후 진행 또는 취소

**예시**:
```
⚠️ 재고 부족 경고

투입재료: 냉연 코일 1.2T
필요 수량: 100개
현재 재고: 80개
부족 수량: 20개
안전 재고: 50개
예상 부족일: 2일 후

계속 진행하시겠습니까?
```

**기대 효과**:
- ✅ 재고 부족 사전 방지
- ✅ 생산 계획 개선
- ✅ 안전 재고 관리

---

### 💡 우선순위 3: 중장기 구현 권장

#### 5. 작업자별 평균 수율 기반 자동 계산

**제안 기능**: 작업자 선택 시 해당 작업자의 평균 수율을 기반으로 산출수량 자동 계산

**작동 방식**:
1. 작업자 선택
2. 해당 작업자의 과거 작업 이력 조회
3. 평균 수율 계산
4. 산출수량 자동 계산

**예시**:
- 작업자: "김철수" 선택
- 김철수의 평균 수율: 97%
- 투입수량: 100
- 자동 계산된 산출수량: 97

**기대 효과**:
- ✅ 작업자별 수율 추적
- ✅ 개인별 목표 설정 가능
- ✅ 생산성 분석

---

#### 6. 작업 완료 시 품질 검사 자동 생성

**제안 기능**: 작업 완료 시 품질 검사 항목 자동 생성

**작동 방식**:
1. 공정 작업 완료 처리
2. 품질 검사 항목 조회 (BOM 또는 설정)
3. 품질 검사 작업 자동 생성
4. 검사 완료 후 재고 반영

**기대 효과**:
- ✅ 품질 검사 누락 방지
- ✅ 품질 관리 자동화

---

#### 7. 일일 작업량 자동 집계 및 리포트

**제안 기능**: 일일 작업량을 자동으로 집계하고 리포트 생성

**작동 방식**:
1. 매일 자정에 전일 작업량 집계
2. 작업자별, 공정별, 품목별 통계 생성
3. 리포트 자동 생성 및 이메일 발송

**기대 효과**:
- ✅ 일일 생산량 추적
- ✅ 작업자별 성과 분석
- ✅ 생산 계획 개선

---

## 구현 상세

### 기능 1: 과거 수율 기반 자동 산출수량 계산

#### API 엔드포인트

**`GET /api/process-operations/average-efficiency`**

```typescript
// Query Parameters:
// - input_item_id: number
// - output_item_id: number
// - operation_type?: 'BLANKING' | 'PRESS' | 'ASSEMBLY'
// - operator_id?: number (선택사항)
// - days?: number (기간, 기본값: 30일)

// Response:
{
  success: true,
  data: {
    average_efficiency: 95.5,  // 평균 수율 (%)
    sample_count: 20,          // 샘플 수
    min_efficiency: 92.0,      // 최소 수율
    max_efficiency: 98.0,      // 최대 수율
    suggested_output_quantity: 95.5  // 제안 산출수량
  }
}
```

#### 프론트엔드 구현

```typescript
// ProcessOperationForm.tsx에 추가

useEffect(() => {
  if (formData.input_item_id && formData.output_item_id && formData.input_quantity > 0) {
    // 과거 평균 수율 조회
    fetch(`/api/process-operations/average-efficiency?input_item_id=${formData.input_item_id}&output_item_id=${formData.output_item_id}&operation_type=${formData.operation_type}`)
      .then(res => res.json())
      .then(data => {
        if (data.success && data.data.average_efficiency) {
          const suggestedOutput = formData.input_quantity * (data.data.average_efficiency / 100);
          setSuggestedOutputQuantity(suggestedOutput);
          // 산출수량 필드에 회색으로 표시 (자동 입력 가능)
        }
      });
  }
}, [formData.input_item_id, formData.output_item_id, formData.input_quantity]);
```

---

### 기능 2: 코일 스펙 기반 자동 투입수량 계산

#### API 엔드포인트

**`GET /api/coil-specs/[item_id]/calculate-quantity`**

```typescript
// Query Parameters:
// - total_weight?: number (코일 총 중량, kg)
// - target_quantity?: number (목표 수량)

// Response:
{
  success: true,
  data: {
    weight_per_piece: 0.47,      // EA중량 (kg/개)
    max_quantity: 2128,          // 최대 가능 수량
    suggested_input_quantity: 100,  // 제안 투입수량
    remaining_weight: 53        // 잔여 중량
  }
}
```

#### 프론트엔드 구현

```typescript
// ProcessOperationForm.tsx에 추가

useEffect(() => {
  if (formData.input_item_id && formData.operation_type === 'BLANKING') {
    // 코일 스펙 조회
    fetch(`/api/coil-specs?item_id=${formData.input_item_id}`)
      .then(res => res.json())
      .then(data => {
        if (data.success && data.data.length > 0) {
          const coilSpec = data.data[0];
          // 코일 스펙 기반 자동 계산
          const weightPerPiece = coilSpec.weight_per_piece;
          const currentStock = inputItemStock; // 현재 재고 (중량 단위로 가정)
          const suggestedQuantity = Math.floor(currentStock / weightPerPiece);
          setSuggestedInputQuantity(suggestedQuantity);
        }
      });
  }
}, [formData.input_item_id, inputItemStock]);
```

---

### 기능 3: 작업 완료 시 다음 공정 자동 생성

#### 데이터베이스 트리거 또는 API 로직

```typescript
// process-operations/[id]/complete/route.ts에 추가

// 작업 완료 후
if (updatedOp.auto_next_operation && updatedOp.next_operation_type) {
  // 다음 공정 자동 생성
  const nextOperation = {
    operation_type: updatedOp.next_operation_type,
    input_item_id: updatedOp.output_item_id,  // 현재 작업의 산출제품
    output_item_id: getNextOutputItem(updatedOp),  // BOM 또는 설정에서 조회
    input_quantity: updatedOp.output_quantity,
    output_quantity: calculateSuggestedOutput(updatedOp),
    status: 'PENDING',
    chain_id: updatedOp.chain_id,
    chain_sequence: (updatedOp.chain_sequence || 0) + 1,
    parent_operation_id: updatedOp.operation_id
  };
  
  await supabase.from('process_operations').insert(nextOperation);
}
```

---

## 예상 효과

### 정량적 효과

| 기능 | 시간 절감 | 오류 감소 | 사용자 만족도 |
|------|----------|----------|--------------|
| 과거 수율 기반 자동 계산 | 50% | 30% | ⭐⭐⭐⭐⭐ |
| 코일 스펙 기반 자동 계산 | 80% | 50% | ⭐⭐⭐⭐⭐ |
| 다음 공정 자동 생성 | 70% | 40% | ⭐⭐⭐⭐ |
| 재고 부족 자동 알림 | - | 60% | ⭐⭐⭐⭐ |

### 정성적 효과

- ✅ 사용자 입력 최소화
- ✅ 계산 오류 방지
- ✅ 작업 효율성 향상
- ✅ 데이터 일관성 향상
- ✅ 생산 계획 개선

---

## 구현 우선순위

### Phase 1 (즉시 구현)
1. ✅ 과거 수율 기반 자동 산출수량 계산
2. ✅ 코일 스펙 기반 자동 투입수량 계산

**예상 기간**: 2-3일

### Phase 2 (단기 구현)
3. ✅ 작업 완료 시 다음 공정 자동 생성
4. ✅ 재고 부족 시 자동 알림

**예상 기간**: 3-5일

### Phase 3 (중장기 구현)
5. ✅ 작업자별 평균 수율 기반 자동 계산
6. ✅ 작업 완료 시 품질 검사 자동 생성
7. ✅ 일일 작업량 자동 집계 및 리포트

**예상 기간**: 1-2주

---

## 결론

현재 시스템에 추가로 자동화할 수 있는 기능들이 많이 있습니다. 특히 **과거 수율 기반 자동 계산**과 **코일 스펙 기반 자동 계산**은 즉시 구현하여 사용자 편의성을 크게 향상시킬 수 있습니다.

---

**문서 작성**: Claude (Backend System Architect)  
**최종 검토**: 2025-01-21

